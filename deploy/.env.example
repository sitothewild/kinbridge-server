# KinBridge server deploy config
# Copy to .env on the host before `docker compose up -d`

# Hostname or IP that clients will use to reach the relay (hbbr).
# For LAN-only use, this is the Olares box's LAN IP.
# For public access, set to the public DNS name.
RELAY_HOST=192.168.68.54
