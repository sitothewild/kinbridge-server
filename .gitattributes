* text=auto

# Files that must run on Linux hosts regardless of the checkout OS.
# Without LF-pinning, Git on Windows would CRLF-ify these and the
# shebang / unit parsers would break when copied to the server.
*.sh          text eol=lf
*.service     text eol=lf
*.env.example text eol=lf
